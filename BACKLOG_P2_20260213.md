# Backlog Ejecutable P2 - SistemaLegal
Fecha base: 2026-02-13
Estado actual: Fase 2 cerrada (P2-01/P2-02/P2-03/P2-04/P2-05 completados)

## Objetivo operativo (Fase 2)
- Endurecer operacion DB-first para procesos diarios y de release.
- Reducir fallas tardias (timeouts/suites fallidas sin diagnostico temprano).
- Mejorar continuidad operativa con criterios de salida claros por script.

## Orden exacto de ejecucion
1. P2-01
2. P2-02
3. P2-03
4. P2-04
5. P2-05

## Tickets P2

### P2-01 - Preflight DB en procesos operativos (Gate + Nightly Audit)
- Prioridad: alta
- Estimacion: 4h
- Dependencias: P1-01, P1-02
- Archivos objetivo: `db/release_gate.py`, `db/nightly_audit.py`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`, `db/README.md`
- Entregable:
  - `release_gate` valida conectividad DB antes de suites DB.
  - `nightly_audit` falla temprano con causa explicita (`stage`, `last_error`).
- Criterios de aceptacion:
  - Sin `DATABASE_URL`: suites DB `BLOCKED`, gate en `FAIL`.
  - Con `DATABASE_URL` pero DB caida: suites DB `BLOCKED`, gate en `FAIL` con causa operacional.
  - `nightly_audit` retorna codigo != 0 si preflight DB falla.
- Estado: completado (2026-02-13)

### P2-02 - Runner operativo diario (Task Scheduler)
- Prioridad: alta
- Estimacion: 5h
- Dependencias: P2-01
- Archivos objetivo: `RUN_ERP.ps1`, `db/nightly_audit.py`, `db/release_gate.py`, `docs/`
- Entregable:
  - script unico para ejecutar auditoria y gate con logs timestamp.
  - codigos de salida estables para automatizacion.
- Criterios de aceptacion:
  - genera log diario en `logs/ops_YYYYMMDD.log`.
  - corta flujo si falla preflight o gate.
- Estado: completado (2026-02-13)

### P2-03 - Alertas de degradacion en Auditoria
- Prioridad: media
- Estimacion: 6h
- Dependencias: P2-01
- Archivos objetivo: `views.py`, `audit.py`
- Entregable:
  - alerta visual cuando errores/warnings empeoran vs promedio ultimos 7 dias.
- Criterios de aceptacion:
  - compara dia actual vs baseline 7 dias.
  - muestra severidad (leve/moderada/critica) y sugerencia accionable.
- Estado: completado (2026-02-13)

### P2-04 - Export operativo de hallazgos
- Prioridad: media
- Estimacion: 4h
- Dependencias: P2-03
- Archivos objetivo: `views.py`, `audit.py`, `exports.py`
- Entregable:
  - export CSV/JSON de hallazgos filtrados por nivel/codigo/fecha.
- Criterios de aceptacion:
  - permite descarga desde UI Auditoria.
  - incluye metadata de snapshot y backend.
- Estado: completado (2026-02-13)

### P2-05 - Cobertura de pruebas operativas P2
- Prioridad: alta
- Estimacion: 6h
- Dependencias: P2-01, P2-03, P2-04
- Archivos objetivo: `db/contract_test.py`, `db/smoke_test.py`, `db/ux_phase2_test.py`
- Entregable:
  - pruebas para preflight/fallas controladas y alertas/export.
- Criterios de aceptacion:
  - sin regresion en suites core.
  - al menos 1 test por feature P2 nueva.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/nightly_audit.py` PASS con DB local activa (`localhost:5432`).
  - `python db/release_gate.py` PASS con suites core en verde.

## Tickets P3

### P3-01 - Hardening UI (`use_container_width` -> `width`)
- Prioridad: alta
- Estimacion: 3h
- Dependencias: P2-05
- Archivos objetivo: `app.py`, `ui.py`, `views.py`, `grids.py`, `db/contract_test.py`
- Entregable:
  - migracion runtime a `width="stretch"` en componentes Streamlit con warning deprecado.
  - guardrail estatico en `contract_test` para bloquear regresiones.
- Criterios de aceptacion:
  - 0 ocurrencias de `use_container_width` en runtime.
  - suites core operativas en PASS.
  - `release_gate` sin warning deprecado de `use_container_width`.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - migracion aplicada en runtime (`app.py`, `ui.py`, `views.py`, `grids.py`).
  - test `streamlit_width_hardening` agregado en `db/contract_test.py`.
  - validacion `release_gate` sin warnings deprecados por `use_container_width`.

### P3-02 - Alineacion DB-first (roadmap + documentacion operativa)
- Prioridad: alta
- Estimacion: 6h
- Dependencias: P3-01
- Archivos objetivo: `BACKLOG_P2_20260213.md`, `db/README.md`, `db/schema.sql`, `docs/README.md`
- Entregable:
  - eliminar contradicciones FS-vs-DB en backlog/docs.
  - dejar estado operativo explicito: backend DB-first (`repo.py -> repo_db.py`).
  - reemplazar `P3-02 - TBD` por ticket ejecutable.
- Criterios de aceptacion:
  - no quedan leyendas "NO INTEGRADO" ni "app usa filesystem" en docs operativas.
  - el backlog P3 deja proximo ticket definido con dependencias.
  - `contract_test` continua PASS.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - documentos operativos alineados a DB-first.
  - roadmap P3 actualizado sin placeholders.

### P3-03 - Timeouts operativos en Gate y DailyOps
- Prioridad: alta
- Estimacion: 10h
- Dependencias: P3-02
- Archivos objetivo: `db/release_gate.py`, `RUN_ERP.ps1`, `db/contract_test.py`, `docs/MANUAL_ADMINISTRACION.md`, `db/README.md`
- Entregable:
  - timeout por suite en `release_gate`.
  - watchdog de proceso para `RUN_ERP.ps1 -DailyOps`.
  - salida con causa explicita cuando hay timeout.
- Criterios de aceptacion:
  - ninguna suite puede quedar bloqueada indefinidamente.
  - `DailyOps` corta flujo con codigo de salida estable ante timeout.
  - `contract_test` valida el contrato de ejecucion actualizado.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `db/release_gate.py` aplica timeout por suite via `VG_SUITE_TIMEOUT_SEC` (default `900`).
  - `RUN_ERP.ps1 -DailyOps` aplica watchdog por paso via `VG_STEP_TIMEOUT_SEC` (default `1200`).
  - `python db/contract_test.py` PASS (`17/17`).

### P3-04 - Aislamiento de suites DB (entorno de prueba dedicado)
- Prioridad: alta
- Estimacion: 8h
- Dependencias: P3-03
- Archivos objetivo: `db/test_env.py`, `db/release_gate.py`, `db/smoke_test.py`, `db/ux_phase2_test.py`, `db/ux_gestion_regression_test.py`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - contrato explicito de DB de test via `VG_TEST_DATABASE_URL`.
  - suites DB bloquean ejecucion si el entorno apunta a DB no aislada.
  - `release_gate` ejecuta suites DB con env aislado y preflight sobre DB de test.
- Criterios de aceptacion:
  - `ux_*` y `smoke_test` fallan temprano si falta `VG_TEST_DATABASE_URL`.
  - `VG_TEST_DATABASE_URL` no puede coincidir con `DATABASE_URL`.
  - `contract_test` incluye guardrail estatico + caso negativo del contrato.
- Estado: completado (2026-02-13)

### P3-05 - Release gate seguro por modo (`read_only`/`full`)
- Prioridad: alta
- Estimacion: 6h
- Dependencias: P3-04
- Archivos objetivo: `db/release_gate.py`, `db/contract_test.py`, `db/smoke_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - `release_gate` soporta modo explicito `read_only`/`full` por CLI (`--mode`) y env (`VG_RELEASE_GATE_MODE`).
  - modo `read_only`: ejecuta suites de lectura y marca suites DB como `SKIPPED` (sin bloquear release).
  - modo `full` preserva contrato P3-04 (aislamiento DB de pruebas + preflight + `BLOCKED` ante incumplimiento).
- Criterios de aceptacion:
  - `python db/release_gate.py --mode read_only` retorna `PASS` y deja suites DB en `SKIPPED`.
  - `python db/release_gate.py --mode full` mantiene ejecucion completa y contrato de aislamiento P3-04.
  - `contract_test` y `smoke_test` agregan cobertura explicita del contrato por modo.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/contract_test.py` PASS (`20/20`).
  - `python db/smoke_test.py` PASS (`9/9`) incluyendo `release_gate_read_only_missing_test_dsn`.
  - `python db/release_gate.py` PASS en modo `full`.
  - `python db/release_gate.py --mode read_only` PASS con suites DB `SKIPPED`.

### P3-06 - Contrato de entorno reproducible + sync documental
- Prioridad: alta
- Estimacion: 6h
- Dependencias: P3-05
- Archivos objetivo: `db/env_contract.py`, `.env.example`, `RUN_ERP.ps1`, `db/contract_test.py`, `db/smoke_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - contrato ejecutable de entorno por perfil (`app`, `db_suite`, `release_gate_full`, `release_gate_read_only`, `daily_ops`).
  - plantilla `.env.example` con variables operativas y de test.
  - `RUN_ERP.ps1 -DailyOps` valida contrato de entorno antes de nightly/gate.
  - guardrails estaticos y de smoke para bloquear drift entre scripts/docs.
- Criterios de aceptacion:
  - `python db/env_contract.py --profile daily_ops` retorna `PASS` con entorno valido.
  - `daily_ops` corta flujo si contrato de entorno falla.
  - docs operativas quedan sincronizadas con variables/contrato y comandos reales.
  - `contract_test`, `ux_phase2_test`, `smoke_test` y `release_gate` en `PASS`.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/contract_test.py` PASS (incluye `env_contract_reproducible_docs_sync`).
  - `python db/ux_phase2_test.py` PASS.
  - `python db/smoke_test.py` PASS (incluye pruebas `env_contract daily_ops`).
  - `python db/release_gate.py` PASS.
  - `python db/env_contract.py --profile daily_ops` PASS.

### P4-01 - Tests operativos conductuales
- Prioridad: alta
- Estimacion: 8h
- Dependencias: P3-06
- Archivos objetivo: `db/ops_behavior_test.py`, `db/smoke_test.py`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - suite conductual para validar rutas operativas reales (`env_contract` + `RUN_ERP.ps1 -DailyOps`).
  - casos de corte por entorno invalido y caso PASS en `read_only` sin DB de test.
  - smoke y contract con guardrails para evitar regresiones de cobertura operacional.
- Criterios de aceptacion:
  - `python db/ops_behavior_test.py` PASS en entorno local DB-first.
  - `smoke_test` ejecuta suite conductual y falla si no pasa.
  - `contract_test` valida existencia/alcance de la suite conductual.
  - docs operativas incluyen comando de verificacion conductual.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/ops_behavior_test.py` PASS (`4/4`).
  - `python db/smoke_test.py` PASS (incluye `ops_behavior_suite`).
  - `python db/contract_test.py` PASS (incluye `ops_behavior_suite_contract`).
  - `python db/release_gate.py` PASS sin regresiones.

### P4-02 - CI con PostgreSQL efimero
- Prioridad: alta
- Estimacion: 6h
- Dependencias: P4-01
- Archivos objetivo: `.github/workflows/ci-db.yml`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - workflow CI DB-first con servicio PostgreSQL efimero.
  - bootstrap de DB runtime/test + aplicacion idempotente de `db/schema.sql`.
  - validacion de contrato de entorno (`release_gate_full`) y ejecucion de `release_gate --mode full`.
  - guardrail estatico en `contract_test` para detectar drift del workflow.
- Criterios de aceptacion:
  - CI ejecuta `release_gate` en modo `full` con `VG_TEST_DATABASE_URL` dedicada.
  - schema aplicado sobre DB runtime y DB test en cada corrida.
  - `contract_test` valida existencia/tokens criticos del workflow.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/contract_test.py` PASS (incluye `ci_postgres_ephemeral_contract`).
  - `python db/release_gate.py --mode full` PASS sobre entorno local aislado.

### P4-03 - Observabilidad estructurada (`run_id`, `stage`, `suite`)
- Prioridad: alta
- Estimacion: 6h
- Dependencias: P4-02
- Archivos objetivo: `db/release_gate.py`, `db/nightly_audit.py`, `RUN_ERP.ps1`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - emision de eventos estructurados `[OBS]` en gate/nightly/DailyOps.
  - correlacion por `run_id` compartido via env `VG_RUN_ID`.
  - etiquetas `stage` y `suite` para diagnostico rapido de fallas.
  - guardrail estatico en `contract_test` para bloquear drift de observabilidad.
- Criterios de aceptacion:
  - `release_gate` y `nightly_audit` muestran `run_id`, `stage`, `suite` en salida.
  - `RUN_ERP.ps1 -DailyOps` registra eventos estructurados en `logs/ops_YYYYMMDD.log`.
  - `contract_test` valida tokens criticos de observabilidad en scripts/docs.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/contract_test.py` PASS (incluye `structured_observability_contract`).
  - `python db/release_gate.py --mode full` PASS con eventos `[OBS]`.
  - `RUN_ERP.ps1 -DailyOps` PASS y registra `run_id/stage/suite`.

### P4-04 - Quality gate KPI
- Prioridad: alta
- Estimacion: 6h
- Dependencias: P4-03
- Archivos objetivo: `db/release_gate.py`, `db/env_contract.py`, `.env.example`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - `release_gate` incorpora quality gate KPI con modo configurable `off|warn|enforce`.
  - KPI gate usa objetivos operativos historicos (`FECHA_TAREA`, `EXPEDIENTE`, `EVENTO/FECHA_EVENTO`, `COBERTURA_FINANCIERA`).
  - en `warn` informa desvio sin bloquear release; en `enforce` bloquea release.
  - `env_contract` valida variables KPI (`VG_QUALITY_GATE_KPI_MODE`, `VG_QUALITY_GATE_KPI_MIN_CASES`).
  - `contract_test` agrega guardrail estatico `quality_gate_kpi_contract`.
- Criterios de aceptacion:
  - `python db/release_gate.py --mode full` muestra bloque `QUALITY GATE KPI` y eventos `[OBS]` con `stage="kpi_gate"`.
  - el gate mantiene compatibilidad con `read_only` (KPI en `SKIPPED`).
  - docs operativas y `.env.example` sincronizan variables KPI.
  - `contract_test` y `ops_behavior_test` permanecen en PASS.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/contract_test.py` PASS (`25/25`, incluye `quality_gate_kpi_contract`).
  - `python db/ops_behavior_test.py` PASS (`4/4`).
  - `python db/release_gate.py --mode full` PASS con `quality_gate_kpi` en `WARN` (modo default `warn`) y `stage="kpi_gate"`.
  - `python db/env_contract.py --profile release_gate_full` PASS validando contrato KPI.

## Tickets P5

### P5-01 - Extraccion legado FS + modularizacion
- Prioridad: alta
- Estimacion: 8h
- Dependencias: P4-04
- Archivos objetivo: `app.py`, `views.py`, `audit.py`, `db/contract_test.py`
- Entregable:
  - runtime principal sin import directo de `fs_repo`.
  - bootstrap de `app.py` consolidado DB-first (`repo.py -> repo_db.py`).
  - flujos de edicion/auditoria sin ramas legacy filesystem para diagnostico operativo.
  - guardrail estatico `legacy_fs_extraction_contract` en `contract_test`.
- Criterios de aceptacion:
  - `app.py`, `views.py`, `audit.py` no importan `fs_repo` en runtime.
  - `app.py` no conserva bootstrap legacy (`GestorCasos(RUTA_BASE)`, `RUTA_BASE_AUTO_CREATE`, `_show_ruta_error`).
  - `contract_test`, `ops_behavior_test` y `release_gate --mode full` permanecen en PASS.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python -m py_compile app.py views.py audit.py db/contract_test.py` PASS.
  - `python db/contract_test.py` PASS (`26/26`, incluye `legacy_fs_extraction_contract`).
  - `python db/ops_behavior_test.py` PASS (`4/4`).
  - `python db/release_gate.py --mode full` PASS sin regresiones del gate.

### P5-02 - Seguridad base (auth/roles/least privilege)
- Prioridad: alta
- Estimacion: 10h
- Dependencias: P5-01
- Archivos objetivo: `db/security_baseline.py`, `db/release_gate.py`, `db/env_contract.py`, `.env.example`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - baseline de seguridad reusable para auditar auth/roles/least-privilege en DB runtime/test.
  - `release_gate` integra security gate con modo configurable `off|warn|enforce`.
  - contrato de entorno valida variables de seguridad y formato de roles esperados.
  - guardrail estatico `security_baseline_contract` bloquea drift de codigo/docs/env.
- Criterios de aceptacion:
  - `python db/release_gate.py --mode full` muestra bloque `SECURITY GATE` y eventos `[OBS]` con `stage="security_gate"`.
  - `VG_SECURITY_GATE_MODE=warn` informa desvio sin bloquear release.
  - `VG_SECURITY_GATE_MODE=enforce` bloquea release ante desvio de privilegios.
  - `.env.example`, `db/README.md`, `docs/README.md` y `docs/MANUAL_ADMINISTRACION.md` sincronizan variables de seguridad.
  - `contract_test` y `ops_behavior_test` permanecen en PASS.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python -m py_compile db/security_baseline.py db/release_gate.py db/env_contract.py db/contract_test.py` PASS.
  - `python db/contract_test.py` PASS (incluye `security_baseline_contract`).
  - `python db/ops_behavior_test.py` PASS (`4/4`).
  - `python db/release_gate.py --mode full` PASS con `security_gate` en `WARN` (modo default `warn`).

### P5-03 - Backup + restore drills
- Prioridad: alta
- Estimacion: 8h
- Dependencias: P5-02
- Archivos objetivo: `db/backup_restore_drill.py`, `db/env_contract.py`, `.env.example`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - script DB-first para backup logico JSON de tablas core.
  - restore drill sobre schema temporal en DB de test (`VG_TEST_DATABASE_URL`) con validacion de conteos.
  - limpieza automatica de schema temporal por default (no destructivo).
  - perfil de contrato `backup_restore_drill` en `env_contract`.
  - guardrail estatico `backup_restore_drill_contract` en `contract_test`.
- Criterios de aceptacion:
  - `python db/backup_restore_drill.py` finaliza `PASS` con backup + restore drill completo.
  - restore drill valida aislamiento (DB test dedicada) y no toca DB runtime.
  - `.env.example` y docs operativas sincronizan variables `VG_BACKUP_DIR` y `VG_BACKUP_DRILL_SCHEMA_PREFIX`.
  - `contract_test`, `ops_behavior_test` y `release_gate --mode full` permanecen en PASS.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python db/env_contract.py --profile backup_restore_drill` PASS.
  - `python db/backup_restore_drill.py` PASS (backup + restore drill).
  - `python db/contract_test.py` PASS (incluye `backup_restore_drill_contract`).
  - `python db/ops_behavior_test.py` PASS (`4/4`).
  - `python db/release_gate.py --mode full` PASS sin regresiones de gate.

### P5-04 - Performance/capacidad DB
- Prioridad: media
- Estimacion: 8h
- Dependencias: P5-03
- Archivos objetivo: `db/performance_capacity.py`, `db/release_gate.py`, `db/env_contract.py`, `.env.example`, `db/contract_test.py`, `db/README.md`, `docs/README.md`, `docs/MANUAL_ADMINISTRACION.md`
- Entregable:
  - baseline reusable `db/performance_capacity.py` para auditar latencia/capacidad runtime.
  - `release_gate` integra performance gate configurable `off|warn|enforce`.
  - contrato de entorno valida variables de performance y umbrales.
  - guardrail estatico `performance_capacity_contract` en `contract_test`.
- Criterios de aceptacion:
  - `python db/release_gate.py --mode full` muestra bloque `PERFORMANCE/CAPACITY GATE` y eventos `[OBS]` con `stage="performance_gate"`.
  - `VG_PERFORMANCE_GATE_MODE=warn` informa desvio sin bloquear release.
  - `VG_PERFORMANCE_GATE_MODE=enforce` bloquea release ante desvio de performance/capacidad.
  - `.env.example`, `db/README.md`, `docs/README.md` y `docs/MANUAL_ADMINISTRACION.md` sincronizan variables de performance gate.
  - `contract_test` y `ops_behavior_test` permanecen en PASS.
- Estado: completado (2026-02-13)
- Evidencia operativa:
  - `python -m py_compile db/performance_capacity.py db/release_gate.py db/env_contract.py db/contract_test.py` PASS.
  - `python db/performance_capacity.py --mode warn` PASS.
  - `python db/env_contract.py --profile release_gate_full` PASS.
  - `python db/contract_test.py` PASS (incluye `performance_capacity_contract`).
  - `python db/ops_behavior_test.py` PASS (`4/4`).
  - `python db/release_gate.py --mode full` PASS con `performance_gate` en `PASS` (modo default `warn`).

## Siguiente ticket
- Roadmap actual completado hasta P5-04. Definir fase siguiente (P6).
